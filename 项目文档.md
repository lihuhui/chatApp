项目文档：基于微信聊天记录的 AI 模拟对话（v1.0 MVP）

一、项目概述

1.1 项目名称
AI 模拟对话 - 微信风格聊天模拟器

1.2 项目简介
基于用户提供的真实微信聊天记录，通过 AI 学习对方的说话风格和语言习惯，实现风格模拟对话体验。MVP 阶段包含：
- App 端（iOS + Android）：完整功能（导入聊天记录、创建角色、AI 对话）
- 微信小程序（轻量版）：示例体验 + 已有角色对话 + 历史回顾

1.3 项目目标
- 完成 MVP 版本开发，App 端上架 iOS App Store 和主流 Android 应用商店，小程序端上架微信小程序平台
- 核心功能可用率达到 95% 以上
- AI 云端回复响应时间 < 3 秒
- 本地风格特征提取 < 2 秒
- 用户导入聊天记录成功率 > 90%

1.4 项目周期
- 技术验证阶段：X 周
- 开发阶段：X 周
- 测试阶段：X 周
- 上线准备：X 周
总周期：X 周（待评估后确定）

二、技术方案

2.1 技术栈选型

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端框架 | uni-app (Vue 3) | 跨端开发，一套代码覆盖 iOS、Android 和微信小程序；版本锁定当前稳定版 |
| 状态管理 | Pinia | uni-app 官方推荐，替代 Vuex |
| 样式 | SCSS | 配合 rpx 单位实现跨端适配 |
| UI 组件 | 自研微信风格聊天组件 | 绿气泡、白气泡、头像、昵称等核心聊天 UI 组件 |
| 本地存储 | SQLite + 文件系统 | SQLite 存储结构化数据（角色元信息、对话历史等），文件系统存储风格特征文件 |
| 本地特征提取 | 规则引擎（词频统计/正则/字数分布）+ 可选轻量 NLP 模型 | 纯 JS 实现核心统计逻辑（词频、口头禅、句长等），无需端侧模型即可完成；后续可叠加轻量 NLP 模型提升语气分析精度 |
| 本地降级推理 | Llama 3.2 1B / Gemma 2B 等端侧小模型 | 离线模式下提供基础回复能力（不加载风格特征或弱化风格） |
| 云端 AI 生成 | OpenAI API / Claude API / 国产大模型 API | 基于本地提取的风格特征生成风格化回复 |
| 后端服务 | Node.js / Python + 云函数 | 转发层：接收风格特征 + 用户消息，调用大模型 API，返回生成结果 |
| 云端存储 | 云数据库 | 持久化风格特征文本（不可逆）和对话历史，用于跨端同步 |
| 跨端同步 | 云端 API | App 端上传风格特征和对话历史；小程序端通过云端 API 读取 |

2.2 架构设计

┌─────────────────────────────────────────────────┐
│                 客户端 - App 端                    │
│  ┌───────────┐  ┌───────────┐  ┌─────────────┐  │
│  │ 聊天记录   │  │ 本地特征   │  │  本地降级    │  │
│  │ 解析引擎   │  │ 提取模块   │  │  推理引擎    │  │
│  └───────────┘  └───────────┘  └─────────────┘  │
│  ┌───────────┐  ┌───────────┐  ┌─────────────┐  │
│  │  UI 层    │  │  SQLite   │  │  文件存储    │  │
│  │ (uni-app) │  │  数据库   │  │ (特征文件)   │  │
│  └───────────┘  └───────────┘  └─────────────┘  │
│                      │                            │
│         原始数据不上云 │  上传：风格特征 + 对话历史    │
└──────────────────────┼────────────────────────────┘
                       │
┌──────────────────────┼────────────────────────────┐
│                 客户端 - 小程序端                    │
│  ┌────────────────────────────────────────────┐  │
│  │             UI 层 (uni-app)                 │  │
│  │  示例角色体验 / 角色列表 / 聊天界面 / 历史回顾  │  │
│  └────────────────────┬───────────────────────┘  │
│                       │ 读取：风格特征 + 对话历史    │
└───────────────────────┼──────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────┐
│                    云端服务层                      │
│  ┌──────────────────────────────────────────┐   │
│  │            API 转发服务（云函数）           │   │
│  │  接收：风格特征 + 上下文 + 用户消息         │   │
│  │  处理：构建 prompt，调用大模型 API          │   │
│  │  返回：生成回复                            │   │
│  └──────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────┐   │
│  │              云数据库                      │   │
│  │  存储：风格特征文本 + 对话历史              │   │
│  │  用途：跨端同步                            │   │
│  └──────────────────────────────────────────┘   │
│                       │                          │
│                       ▼                          │
│  ┌──────────────────────────────────────────┐   │
│  │           大模型 API（第三方）              │   │
│  │  OpenAI / Claude / 国产大模型              │   │
│  └──────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘

2.3 核心模块设计

2.3.1 聊天记录解析引擎
- 所属端：App 端
- 功能：解析用户粘贴的聊天记录文本
- 输入：原始文本（支持 iOS/Android/PC 微信导出格式）
- 输出：结构化的对话数据（说话人、消息内容、时间戳）
- 处理逻辑：
  a. 自动识别格式类型（iOS 冒号格式 / Android 换行格式 / PC 多种格式）
  b. 正则匹配拆分消息条目
  c. 过滤非对话内容（系统消息、红包、转账、小程序卡片、图片、语音等）
  d. 识别说话人列表
  e. 统计消息数量及过滤数量
- 技术选型注意：纯逻辑处理，不依赖平台 API，代码在所有端复用

2.3.2 本地风格特征提取模块
- 所属端：App 端
- 功能：从结构化对话数据中提取对方的语言风格特征
- 输入：标记了"对方"身份的结构化对话数据
- 输出：风格特征文本（统计级别数据，不可逆）
- 提取维度：
  a. 常用词汇及频率（词频统计）
  b. 常用口头禅（高频短语）
  c. 平均回复长度（字数分布）
  d. 表情符号使用习惯（频率、偏好类型）
  e. 语气特征（口语化程度、语气词使用、标点符号习惯等）
  f. 回复节奏（对话中的响应模式）
- 技术实现：纯 JS 规则引擎（词频统计、正则匹配、字数分布分析），无需依赖端侧模型；后续可叠加轻量 NLP 模型优化语气分析精度
- 注意：初始版本先实现纯 JS 方案快速验证，端侧 NLP 模型作为优化项后续迭代

2.3.3 云端 AI 生成服务
- 功能：基于风格特征生成风格化回复
- 输入：风格特征文本 + 对话上下文（最近 N 轮）+ 用户消息
- 输出：AI 生成的风格化回复
- 调用端：App 端 + 小程序端（均通过云端 API）
- 核心逻辑：
  a. 构建 system prompt：将风格特征注入 prompt，指导大模型模仿目标风格
  b. 管理对话上下文窗口（保留最近 10-15 轮）
  c. 调用大模型 API（设置 temperature、max_tokens 等参数）
  d. 返回生成结果并持久化存储对话历史
- Prompt 工程要点：
  - 风格特征精确嵌入
  - 上下文连贯性维护
  - 防止 OOC（角色崩坏）的约束条件
  - 回复长度控制
- 注意：云端不做原始聊天记录持久化；仅保留风格特征（不可逆）和对话历史

2.3.4 本地降级推理引擎
- 所属端：App 端（小程序端不包含，小程序必须联网）
- 功能：无网络时提供基础回复能力
- 输入：用户消息 + 简短上下文
- 输出：基础回复（不加载风格特征或弱化风格）
- 技术方案：端侧部署轻量级 LLM（如 Llama 3.2 1B、Gemma 2B）
- 触发条件：网络不可用或云端服务超时时自动切换
- 用户提示：切换时提示"当前为离线模式，回复可能不够贴合风格"
- 注意：小程序端不支持此模块，弱网/无网时直接提示"需要网络连接"

2.3.5 跨端同步模块
- 功能：App 端创建的角色和对话记录同步至小程序端
- 同步数据（云端持久化）：
  a. 风格特征文本（不可逆，统计级别）
  b. 角色元信息（昵称、头像）
  c. 对话历史（会话列表、消息内容）
- 不同步数据：原始聊天记录（永不离开设备本地）
- 同步时机：
  - App 端：创建角色时上传风格特征和角色信息；每次对话后增量同步对话记录
  - 小程序端：打开角色列表时从云端拉取；进入对话时拉取历史记录
- 冲突处理：以 App 端数据为准（App 端可写，小程序端只读同步）

2.3.6 数据存储模块

本地存储（App 端，SQLite）：

【角色表 roles】
- id: 主键
- name: 角色昵称
- avatar: 头像路径
- style_features: 风格特征文本（JSON，本地缓存）
- message_count: 导入消息总数
- cloud_sync_id: 云端对应 ID
- created_at: 创建时间
- updated_at: 更新时间

【对话会话表 sessions】
- id: 主键
- role_id: 关联角色 ID
- title: 会话标题（默认取第一条用户消息）
- round_count: 当前轮数
- status: 状态（进行中/已达上限）
- cloud_sync_id: 云端对应 ID
- created_at: 创建时间
- updated_at: 更新时间

【消息表 messages】
- id: 主键
- session_id: 关联会话 ID
- sender: 发送者（user / ai）
- content: 消息内容
- cloud_sync_id: 云端对应 ID
- created_at: 发送时间

云端存储（云数据库）：

【云端角色表 cloud_roles】
- id: 主键
- user_id: 用户标识
- name: 角色昵称
- avatar: 头像 URL
- style_features: 风格特征文本（不可逆）
- created_at: 创建时间
- updated_at: 更新时间

【云端会话表 cloud_sessions】
- id: 主键
- role_id: 关联云端角色 ID
- user_id: 用户标识
- title: 会话标题
- round_count: 当前轮数
- status: 状态
- created_at: 创建时间
- updated_at: 更新时间

【云端消息表 cloud_messages】
- id: 主键
- session_id: 关联云端会话 ID
- sender: 发送者（user / ai）
- content: 消息内容
- created_at: 发送时间

本地存储（小程序端）：
- 仅缓存当前浏览的角色和对话数据，以云端为准
- 小程序端不做本地持久化（数据量小，直接读云端）

2.4 接口设计

2.4.1 云端 AI 生成 API

请求：
{
  "style_features": "风格特征文本（统计级别，不可逆原始对话）",
  "context": [
    {"sender": "user", "content": "..."},
    {"sender": "ai", "content": "..."}
  ],
  "message": "用户当前发送的消息",
  "role_name": "对方昵称",
  "session_id": "会话标识（用于限流计数和对话持久化）"
}

响应：
{
  "reply": "AI 生成的回复内容",
  "message_id": "云端消息 ID",
  "tokens_used": 150
}

错误码：
- 200: 成功
- 429: 调用频率超限
- 500: 服务内部错误
- 503: 服务暂时不可用

2.4.2 跨端同步 API

上传角色（App 端调用）：
请求：
{
  "user_id": "用户标识",
  "name": "角色昵称",
  "avatar": "头像 URL",
  "style_features": "风格特征文本"
}
响应：
{
  "role_id": "云端角色 ID"
}

获取角色列表（小程序端调用）：
请求：
{
  "user_id": "用户标识"
}
响应：
{
  "roles": [
    {
      "role_id": "云端角色 ID",
      "name": "角色昵称",
      "avatar": "头像 URL",
      "last_session_title": "最近会话标题"
    }
  ]
}

同步对话记录（App 端调用）：
请求：
{
  "session_id": "云端会话 ID",
  "messages": [
    {"sender": "user", "content": "..."},
    {"sender": "ai", "content": "..."}
  ]
}
响应：
{
  "synced_count": 2
}

获取对话历史（小程序端调用）：
请求：
{
  "session_id": "云端会话 ID",
  "page": 1,
  "page_size": 50
}
响应：
{
  "messages": [
    {
      "message_id": "云端消息 ID",
      "sender": "user",
      "content": "...",
      "created_at": "时间戳"
    }
  ]
}

2.4.3 限流策略
- 单用户每日 AI 调用上限：100 次/天（MVP 阶段，根据成本调整）
- 单用户每分钟调用上限：10 次/分钟
- 超出限额时返回 429，客户端提示"今日对话次数已用完，请明天再来"

三、开发任务分解

3.1 阶段零：技术验证（预估 X 天）

□ 验证 uni-app 在 iOS/Android/微信小程序的基础运行环境
□ 验证微信聊天记录多格式解析的准确率（收集 50+ 真实样本测试）
□ 验证本地风格特征提取效果（是否准确捕捉语癖、口头禅等）
□ 验证云端大模型风格模仿效果（盲测风格相似度 ≥ 60%）
□ 确认云端 API 成本和限流阈值
□ 验证端侧小模型在 App 端的推理速度和基础对话质量（用于离线降级，非核心功能）

3.2 阶段一：基础框架搭建（预估 X 天）

□ uni-app 项目初始化，配置 Pinia、SCSS
□ 基础页面路由和导航结构搭建
□ 条件编译框架搭建（App 端 / 小程序端差异化）
□ 网络层封装（uni.request 统一封装，错误处理）
□ SQLite 数据库初始化与基础 CRUD 封装
□ 云函数项目初始化
□ 云数据库表结构设计与创建
□ 用户登录与标识体系（App 端 + 小程序端统一用户标识）

3.3 阶段二：App 端核心功能开发（预估 X 天）

□ 聊天记录粘贴导入界面
□ 聊天记录解析引擎（iOS/Android/PC 格式识别与解析）
□ 说话人识别与身份确认流程
□ 本地风格特征提取模块（纯 JS 规则引擎：词频统计、语气分析、回复长度分布等）
□ 风格分析报告展示页
□ 云端 AI 生成 API 对接
□ 对话轮数计数器与上限提示
□ 本地降级推理引擎集成（离线模式）
□ 角色创建与管理（设置页、详情页、重新导入、删除）
□ 对话列表与历史回顾

3.4 阶段三：小程序端核心功能开发（预估 X 天）

□ 小程序端页面结构搭建（条件编译）
□ 示例角色体验流程
□ 角色列表页（从云端同步）
□ 聊天界面（对接云端 AI 生成 API）
□ 对话轮数计数器与上限提示
□ 对话历史回顾页（从云端加载）
□ 下载 App 引导页
□ 小程序包体积优化（不包含端侧模型和解析引擎）
□ 小程序审核材料准备

3.5 阶段四：跨端同步开发（预估 X 天）

□ 云端存储 API 开发（角色、会话、消息 CRUD）
□ App 端上传同步逻辑（创建角色时上传、对话后增量同步）
□ 小程序端拉取同步逻辑（角色列表、对话历史）
□ 同步冲突处理（以 App 端为准）
□ 用户标识统一（跨端用户关联）

3.6 阶段五：UI 与交互开发（预估 X 天）

□ 微信风格聊天界面组件开发（气泡、头像、昵称、时间）
□ 首页（体验示例角色 / 创建新角色入口）
□ 角色创建与设置页
□ 聊天记录导入引导页（分平台操作说明）
□ 风格分析报告展示页
□ 对话列表页（会话历史）
□ 历史对话回顾页（只读模式）
□ 角色详情页（重新导入 / 删除角色）
□ 对话轮数计数器与上限提示组件
□ 网络状态提示与离线模式切换提醒
□ 加载、空状态、错误状态等通用组件

3.7 阶段六：测试与优化（预估 X 天）

□ 聊天记录解析兼容性测试（iOS/Android/PC 真实样本）
□ 云端 AI 生成接口压力测试
□ 弱网/无网降级测试（App 端）
□ 小程序端网络依赖测试
□ App 端多机型适配测试（iOS 主流机型 + Android 主流机型）
□ 小程序端多机型适配测试
□ 跨端同步功能测试
□ 性能测试（AI 回复速度、特征提取速度、小程序包体积）
□ 用户验收测试（UAT）
□ Bug 修复与优化

3.8 阶段七：上线准备（预估 X 天）

□ 隐私政策与用户协议撰写（明确三层数据策略）
□ App Store 审核材料准备
□ Android 应用商店审核材料准备
□ 微信小程序审核材料准备
□ 应用商店截图与描述文案
□ 云服务生产环境部署
□ API 监控与告警配置
□ 成本监控面板搭建
□ 灰度发布方案
□ 正式上架

四、平台差异说明

| 功能 | App 端 | 小程序端 |
|------|--------|----------|
| 示例角色体验 | ✓ | ✓ |
| 与示例角色对话 | ✓ | ✓ |
| 创建新角色 | ✓ | ✗（引导下载 App） |
| 导入聊天记录 | ✓ | ✗ |
| 本地风格特征提取 | ✓ | ✗ |
| 风格分析报告 | ✓ | ✗ |
| 与已创建角色对话 | ✓ | ✓ |
| 对话历史回顾 | ✓ | ✓ |
| 开启新对话 | ✓ | ✓ |
| 重新导入记录 | ✓ | ✗ |
| 删除角色 | ✓ | ✗ |
| 本地降级（离线） | ✓ | ✗（小程序需联网） |
| 跨端同步（上传） | ✓ | ✗（只读拉取） |
| 跨端同步（拉取） | ✓ | ✓ |

五、技术风险与应对

| 风险项 | 风险等级 | 应对措施 |
|--------|----------|----------|
| 微信三种格式解析准确率不达标 | 高 | 技术验证阶段收集 50+ 样本充分测试；导入前增加格式预览区 |
| 云端大模型模仿效果不及预期 | 高 | 技术验证阶段充分调优 prompt；建立风格模仿效果评测标准 |
| 云端 API 调用成本超预算 | 高 | 设置每日单用户调用上限；选择性价比高的模型；持续监控成本 |
| uni-app 集成端侧 AI 模型方案不成熟（仅影响离线降级） | 低 | 端侧模型仅用于离线降级非核心场景，降级不可用时在线优先；使用原生插件封装 |
| 小程序包体积超限（分包限制） | 中 | 小程序端不包含端侧模型和解析引擎；仅集成轻量 UI 和网络层 |
| 跨端同步数据一致性问题 | 中 | 以 App 端为数据源；明确同步时机和冲突解决策略 |
| 微信小程序审核被拒 | 中 | 小程序不涉及创建/导入等敏感功能；准备详细人审材料 |
| App Store 审核被拒 | 中 | 隐私政策明确三层数据策略；提前了解审核政策 |
| 弱网/无网下小程序无法使用 | 低 | 小程序端明确网络要求；弱网时友好提示 |
| 端侧小模型推理速度过慢 | 低 | 选择 1B 以下轻量模型；量化加速；中低端机型实测 |

六、里程碑与交付物

| 里程碑 | 时间节点 | 交付物 |
|--------|----------|--------|
| 技术验证完成 | 第 X 周 | 技术验证报告（解析准确率、风格模仿效果、成本估算、离线降级方案可行性） |
| 基础框架搭建完成 | 第 X 周 | 可运行的空壳项目（路由、网络层、数据库、云函数基础） |
| App 端核心功能完成 | 第 X 周 | App 端 Alpha 版本（导入→提取→对话核心链路） |
| 小程序端核心功能完成 | 第 X 周 | 小程序端 Alpha 版本（示例体验 + 角色同步 + 对话） |
| 跨端同步完成 | 第 X 周 | 跨端数据同步可用，App 端和小程序端数据互通 |
| UI 与交互完成 | 第 X 周 | Beta 版本（完整功能可用，双端 UI 完成） |
| 测试完成 | 第 X 周 | 测试报告 + Bug 修复版本 |
| 上线准备完成 | 第 X 周 | 正式版本 + 审核材料 + 生产环境 |
| 正式上线 | 第 X 周 | App Store + Android 应用商店 + 微信小程序平台 |

七、团队与职责

| 角色 | 职责 | 人数 |
|------|------|------|
| 项目经理 | 项目进度管理、跨团队协调 | 1 |
| uni-app 前端开发 | App 端 + 小程序端前端开发、跨端适配 | 1-2 |
| 后端开发 | 云函数开发、API 转发层、云数据库设计 | 1 |
| AI 工程师 | prompt 工程、风格模仿效果调优、端侧降级模型部署 | 1 |
| UI/UX 设计师 | 界面设计、交互流程 | 1 |
| 测试工程师 | 功能测试、兼容性测试、性能测试 | 1 |

八、附录

8.1 微信聊天记录格式示例

> 以下格式基于真实微信导出格式整理，解析引擎需全部兼容。

iOS 格式（昵称 + 完整日期时间）：
张三 2024/1/15 14:30:22
昨天那个事情你问了吗
李四 2024/1/15 14:31:05
还没呢，明天再说吧
张三 2024/1/15 14:32:18
行吧😅

Android 格式（昵称 + 日期时间，时间格式可能不带秒）：
张三 2024/1/15 14:30
昨天那个事情你问了吗
李四 2024/1/15 14:31
还没呢，明天再说吧

PC 格式（日期时间 + 昵称，顺序与移动端相反）：
2024-01-15 14:30:22 张三
昨天那个事情你问了吗
2024-01-15 14:31:05 李四
还没呢，明天再说吧

8.2 三层数据策略说明

| 数据层级 | 内容 | 存储位置 | 是否上云 | 说明 |
|----------|------|----------|----------|------|
| 原始聊天记录 | 用户粘贴的完整聊天文本 | 设备本地 | 否，永不上云 | 仅用于本地特征提取 |
| 风格特征文本 | 词频、口头禅、平均句长等统计数据 | 本地 + 云端 | 是，云端持久化 | 不可逆，无法还原原始对话 |
| 对话历史 | AI 模拟对话生成的消息记录 | 本地 + 云端 | 是，云端持久化 | 用于跨端同步和多端查看 |

8.3 参考文档
- 产品文档：产品方案 v1.0 正式版·修订
- 技术约束：CLAUDE.md
- 待补充：UI 设计稿链接
- 待补充：接口详细文档链接