# 页面结构与组件文档（v1.0 MVP）

> 基于产品方案 v1.0 正式版和项目文档整理，指导 uni-app 前端页面开发。

---

## 一、页面路由设计

### 1.1 路由总览

```
pages.json 统一注册，平台差异通过条件编译处理
```

| 页面路径 | 页面名称 | App 端 | 小程序端 | 说明 |
|----------|----------|--------|----------|------|
| `pages/index/index` | 首页 | ✓ | ✓ | 双端首页布局不同，条件编译 |
| `pages/chat/chat` | 聊天界面 | ✓ | ✓ | 核心页面，共享组件 |
| `pages/chat/sessions` | 对话列表 | ✓ | ✓ | 历史会话列表 |
| `pages/chat/history` | 对话回顾 | ✓ | ✓ | 只读模式查看历史 |
| `pages/role/create` | 创建角色 | ✓ | ✗ | 设置昵称、头像 |
| `pages/role/import-guide` | 导入引导 | ✓ | ✗ | 分平台导出教程 |
| `pages/role/import-parse` | 导入解析 | ✓ | ✗ | 粘贴+解析+确认身份 |
| `pages/role/style-report` | 风格报告 | ✓ | ✗ | 分析结果展示 |
| `pages/role/detail` | 角色详情 | ✓ | ✗ | 管理角色 |
| `pages/role/list` | 角色列表 | ✗ | ✓ | 从云端同步 |
| `pages/demo/chat` | 示例对话 | ✗ | ✓ | 预设角色体验 |
| `pages/download/index` | 下载引导 | ✗ | ✓ | 引导下载 App |

### 1.2 路由导航图

```
┌──────────────────────────────────────────────────────┐
│                    App 端路由流                       │
├──────────────────────────────────────────────────────┤
│                                                       │
│  首页 ──────────→ 体验示例角色 ───→ 聊天界面           │
│    │                                                  │
│    └──→ 创建角色 ──→ 导入引导 ──→ 导入解析 ──→         │
│                   ──→ 风格报告 ──→ 聊天界面             │
│                                                       │
│  聊天界面 ──→ 对话列表 ──→ 对话回顾                     │
│  聊天界面 ──→ 角色详情（重新导入/删除）                  │
│                                                       │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│                  小程序端路由流                        │
├──────────────────────────────────────────────────────┤
│                                                       │
│  首页 ──────────→ 体验示例角色 ───→ 示例对话            │
│    │                            └──→ 下载引导          │
│    │                                                  │
│    └──→ 角色列表 ──→ 聊天界面 ──→ 对话回顾              │
│                       │                               │
│                       └──→ 下载引导                    │
│                                                       │
└──────────────────────────────────────────────────────┘
```

### 1.3 页面导航方式

| 导航类型 | 实现方式 | 场景 |
|----------|----------|------|
| 标签栏 | `uni.switchTab` | 首页（如需后续扩展标签） |
| 页面跳转 | `uni.navigateTo` | 大部分页面间跳转 |
| 页面返回 | `uni.navigateBack` | 返回上一级 |
| 重定向 | `uni.redirectTo` | 导入完成后跳转聊天 |

---

## 二、组件体系

### 2.1 组件层级

```
┌──────────────────────────────────────────────┐
│                 页面层                         │
│  pages/index/index, pages/chat/chat, ...      │
├──────────────────────────────────────────────┤
│             功能组件层                         │
│  ImportGuide, StyleReportCard, SyncStatusBar  │
├──────────────────────────────────────────────┤
│             业务组件层                         │
│  ChatBubble, ChatInput, RoleCard,             │
│  SpeakerSelector, ParseResultPreview          │
├──────────────────────────────────────────────┤
│             通用组件层                         │
│  Avatar, Loading, EmptyState, ErrorState,     │
│  NetworkAlert, RoundCounter, StatusBadge      │
└──────────────────────────────────────────────┘
```

### 2.2 通用组件（components/common/）

| 组件名 | 功能 | Props | 状态 |
|--------|------|-------|------|
| `Avatar` | 头像显示 | `src, size, name` | loading / loaded / error |
| `Loading` | 加载状态 | `text, type(spinner/dots)` | - |
| `EmptyState` | 空状态占位 | `icon, title, description, actionText` | - |
| `ErrorState` | 错误状态 | `message, retryText, onRetry` | - |
| `NetworkAlert` | 网络状态提示 | `type(warning/error), message` | visible / hidden |
| `RoundCounter` | 轮数计数器 | `current, max` | normal / warning（25+）/ reached（30） |
| `StatusBadge` | 状态标识 | `status(syncing/online/offline/error)` | - |
| `PageContainer` | 页面容器 | `title, showBack, safeArea` | - |
| `ProgressBar` | 进度条 | `percent, text` | - |
| `ConfirmDialog` | 确认弹窗 | `title, message, confirmText, cancelText` | - |

### 2.3 业务组件（components/business/）

| 组件名 | 所属端 | 功能 | 关键 Props |
|--------|--------|------|-----------|
| `ChatBubble` | 共用 | 聊天气泡（绿/白） | `content, sender, time, avatar, nickname` |
| `ChatMessageList` | 共用 | 消息列表 | `messages, loading, typing` |
| `ChatInput` | 共用 | 聊天输入框 | `disabled, placeholder, maxLength` |
| `RoleCard` | 共用 | 角色卡片 | `avatar, name, desc, messageCount` |
| `SessionCard` | 共用 | 会话卡片 | `title, roundCount, lastTime, status` |
| `StyleTag` | 共用 | 风格标签 | `label, type` |
| `SpeakerSelector` | App 端 | 说话人确认 | `speakers[], selected` |
| `ParseResultPreview` | App 端 | 解析结果预览 | `speakerCount, messageCount, filteredCount` |
| `StyleReportCard` | App 端 | 风格报告单项 | `dimension, value, detail` |
| `ImportGuideStep` | App 端 | 导入引导步骤 | `platform, stepIndex, image, text` |
| `SyncStatusBar` | 共用 | 同步状态条 | `status, lastSyncTime` |

### 2.4 功能组件（components/features/）

| 组件名 | 所属端 | 功能 |
|--------|--------|------|
| `ImportGuide` | App 端 | 导入引导页完整内容（含分平台教程） |
| `ImportPasteArea` | App 端 | 粘贴输入区域（含格式示例） |
| `ParseEngine` | App 端 | 解析引擎（纯逻辑，无 UI，放在 utils 层） |
| `FeatureExtraction` | App 端 | 特征提取进度展示（含动画） |
| `StyleReportBoard` | App 端 | 风格分析报告完整面板 |
| `DemoRoleChat` | 小程序端 | 示例角色对话预设 |

---

## 三、Pinia Store 设计

### 3.1 状态模型

```typescript
// store/role.ts — 角色状态
interface RoleState {
  currentRole: Role | null;
  // App 端本地角色列表
  localRoles: Role[];
  // 加载状态
  loading: boolean;
}

// store/chat.ts — 对话状态
interface ChatState {
  currentSession: Session | null;
  messages: Message[];
  roundCount: number;
  isTyping: boolean;
  sessions: Session[];
}

// store/import.ts — 导入流程（App 端专用）
interface ImportState {
  step: 'guide' | 'paste' | 'parsing' | 'confirm' | 'extracting' | 'report';
  rawText: string;
  parseResult: ParseResult | null;
  speakers: Speaker[];
  selectedSpeaker: string | null;
  styleReport: StyleReport | null;
  progress: number;
}

// store/sync.ts — 跨端同步
interface SyncState {
  status: 'idle' | 'syncing' | 'success' | 'error';
  lastSyncTime: string | null;
  pendingCount: number;
}

// store/user.ts — 用户标识
interface UserState {
  userId: string;
  platform: 'app' | 'mp';
}
```

### 3.2 Store 职责划分

| Store | 作用域 | App 端 | 小程序端 | 持久化 |
|-------|--------|--------|----------|--------|
| `useRoleStore` | 全局 | 本地角色 CRUD + 同步 | 从云端拉取 | 是（SQLite/缓存） |
| `useChatStore` | 全局 | 对话管理 + 轮数计数 | 对话管理 + 轮数计数 | 是 |
| `useImportStore` | 临时 | 导入全流程状态 | ✗ | 否（流程结束重置） |
| `useSyncStore` | 全局 | 上传同步 | 拉取同步 | 否 |
| `useUserStore` | 全局 | 用户标识 | 用户标识 | 是 |

---

## 四、关键页面设计

### 4.1 首页（pages/index/index）

```
App 端（暖墨设计语言）：
┌─────────────────────────────────────┐
│  ┌─ 状态栏 (safe area) ───────────┐ │
│  │  09:41                    ▮▮    │ │
│  ├─────────────────────────────────┤ │
│  │                                 │ │
│  │  暖墨                            │ │  ← $font-xxl(48rpx) $ink-text
│  │  聊天风格重塑                    │ │  ← $font-lg(32rpx) $ink-text-secondary
│  │                                 │ │
│  │  ┌───────────────────────────┐  │ │
│  │  │ ▌ 体验示例角色             │  │ │  ← 墨玉青装订线 6rpx
│  │  │   先看看别人是什么风格     │  │ │  卡片: $ink-surface 白底
│  │  │                    →     │  │ │  shadow-sm: 0 2rpx 8rpx rgba(29,29,31,0.06)
│  │  └───────────────────────────┘  │ │  按下: scale(0.98) 0.2s ease
│  │                                 │ │
│  │  ┌───────────────────────────┐  │ │
│  │  │ ▌ 创建新角色               │  │ │  ← 墨玉青装订线
│  │  │   导入聊天记录开始分析     │  │ │  card-gap: 16rpx
│  │  │                    →     │  │ │
│  │  └───────────────────────────┘  │ │
│  │                                 │ │
│  │  v1.0.0                         │ │  ← $font-xs(22rpx) $ink-text-tertiary
│  │  隐私声明 · 用户协议            │ │
│  ├─────────────────────────────────┤ │
│  └─────────────────────────────────┘ │
└─────────────────────────────────────┘

小程序端（暖墨精简版）：
┌─────────────────────────────────────┐
│  ┌─ 自定义导航栏 ─────────────────┐ │
│  │  ← 返回    暖墨                  │ │  ← 墨玉青下边框 2rpx
│  ├─────────────────────────────────┤ │
│  │                                 │ │
│  │  ┌───────────────────────────┐  │ │
│  │  │ ▌ 体验示例角色             │  │ │
│  │  │   先看看别人是什么风格     │  │ │
│  │  └───────────────────────────┘  │ │
│  │                                 │ │
│  │  ┌───────────────────────────┐  │ │
│  │  │ ▌ 我的角色                │  │ │  ← 空状态：引导去 App 创建
│  │  │   由 App 创建同步至此处   │  │ │
│  │  └───────────────────────────┘  │ │
│  │                                 │ │
│  │  [EmptyState 组件: 暂无角色]     │ │  ← 图标 + 引导文字
│  │                                 │ │
│  └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

**设计要点：**
- 卡片左侧 **6rpx 墨玉青装订线**，按下时加深至 `$ink-primary-dark`
- 卡片采用 **白底 + shadow-sm** 投影，区别于纯平铺卡片
- 按钮式进入箭头 `→` 在卡片右侧，轻量提示可交互
- App 端页面两侧留白 **30rpx**，卡片间距 **16rpx**
- 小程序端版本号/隐私声明替换为跨端同步引导
- 使用条件编译 `#ifdef APP-PLUS` / `#ifdef MP-WEIXIN`

### 4.2 聊天界面（pages/chat/chat）

```
┌──────────────────────────────────────┐
│  ← 返回     小暖          ░░░  ···   │  ← NavBar 墨玉青下边框 2rpx
├──────────────────────────────────────┤  bg: $ink-bg #F7F5F0
│                                      │
│  ┌─┐ ┌──────────────────────────┐   │  ← 对方气泡
│  │ │ │ 昨天那个事                │   │     bg: #FFFFFF
│  │头│ │ 你问了吗                  │   │     $radius-lg(20rpx) 左
│  │ │ │                           │   │     $radius-sm(8rpx) 右
│  └─┘ └──────────────────────────┘   │     shadow-sm
│                   14:31              │  ← $ink-text-secondary 22rpx
│                                      │
│  ┌──────────────────────────┐ ┌─┐   │  ← 自己气泡
│  │ 还没呢                    │ │ │   │     bg: $ink-primary #2A9D8F
│  │                          │ │头│   │     $radius-lg(20rpx) 右
│  └──────────────────────────┘ │ │   │     $radius-sm(8rpx) 左
│                   14:32       └─┘   │     文字色: #FFFFFF
│                                      │
│  ┌─┐ ┌──────────────────────────┐   │
│  │ │ │ 行吧 😅                  │   │
│  │头│ └──────────────────────────┘   │
│  └─┘                                 │
│                                      │
│  ┌─┐  ● ○ ○  对方正在输入...        │  ← Typing 指示器
│  └─┘                                 │     墨玉青色三点呼吸动画 1.5s
│                                      │     圆点大小: 8rpx · 间距: 6rpx
│                                      │
│           ▓▓▓▓▓▓░░░░░  15/30 轮      │  ← RoundCounter
│                            已同步     │     25+ 时文字变琥珀金 #E9C46A
│                                      │     30 时文字变珊瑚橙 #E76F51 + 禁用输入
├──────────────────────────────────────┤
│  ┌────────────────────────────┬──┐   │  ← ChatInput
│  │ 输入消息...                 │ ↗│   │     聚焦: $ink-primary 边框 2rpx
│  │                            └──┘   │     $ink-primary-light 背景
│  └─────────────────────────────┘     │     placeholder: $ink-text-tertiary
│      页面两侧留白 30rpx              │
└──────────────────────────────────────┘
```

**核心交互：**

| 交互 | 触发条件 | 反馈 |
|------|----------|------|
| 消息出现 | AI/Avatar 新消息 | 淡入 + 上移 0.3s ease-out |
| 发送失败 | 网络断开 | 气泡水平抖动 0.4s ease-in-out |
| 打字指示 | AI 生成中 | ● ○ ○ 三点交错闪烁（各 delay 0.2s） |
| 轮数预警 | 达到 25 轮 | 计数器变琥珀金 + 文字提示 |
| 轮数上限 | 达到 30 轮 | 输入框禁用显示「已达上限」+ 新对话按钮 |
| 网络变化 | 离线/恢复 | NetworkAlert 从顶部滑入 0.3s ease-out |
| 消息加载 | 进入历史会话 | Skeleton 骨架屏占位 |
| 空状态 | 新对话 | 居中 EmptyState：「开始你的第一次对话」|

**消息类型与气泡状态：**

| 状态 | 视觉特征 | 说明 |
|------|----------|------|
| sending | 气泡右下方小旋转指示器 8rpx | 消息发送中 |
| sent | 无额外标记 | 发送成功 |
| failed | 气泡右下角红色!号 + 可点击重发 | 发送失败 |
| typing | 对方头像旁三点呼吸动画 | AI 正在生成 |
| streaming | 内容逐字显现，光标闪烁 | 流式回复进行中 |

**无障碍：**
- 消息列表每个 item 可被屏幕朗读焦点捕获
- 输入框 `:focus` 状态有 2rpx 墨玉青边框和轻微放大
- 发送按钮 `:active` 状态 scale(0.92) 给予触感反馈

### 4.3 导入解析页（pages/role/import-parse）

```
┌──────────────────────────────────────┐
│  ← 返回     导入聊天记录               │
├──────────────────────────────────────┤
│  bg: $ink-bg                          │
│  ┌──────────────────────────────┐    │
│  │                              │    │  ← ImportPasteArea
│  │   在此粘贴聊天记录...         │    │     聚焦: 墨玉青边框 2rpx
│  │   bg: #FFFFFF                 │    │     $radius-md 12rpx
│  │   shadow-sm inset             │    │     placeholder: $ink-text-tertiary
│  │                              │    │
│  │                              │    │
│  └──────────────────────────────┘    │
│                                      │
│  ┌──┐    ┌──────────┐               │
│  │📋│    │ 格式示例  │               │  ← 折叠引导，点击展开
│  └──┘    └──────────┘               │     墨玉青文字
│                                      │
│  ┌──────────────────────────────┐    │
│  │         开始解析              │    │  ← 主要操作按钮
│  │   bg: $ink-primary            │    │     $radius-md
│  │   文字: #FFFFFF                │    │     按下: scale(0.97) 0.2s
│  │   shadow-sm                    │    │
│  └──────────────────────────────┘    │
│                                      │
│  支持 iOS / Android / PC 导出格式     │  ← $font-sm 24rpx $ink-text-secondary
│  系统消息 · 红包 · 广告 自动过滤      │
└──────────────────────────────────────┘
```

**解析中状态：**

```
┌──────────────────────────────────────┐
│  ← 返回     导入聊天记录               │
├──────────────────────────────────────┤
│                                      │
│  ┌──────────────────────────────┐    │
│  │  ▓▓▓▓▓▓▓▓▓░░░░  75%          │    │  ← ProgressBar
│  │  正在分析聊天记录结构...       │    │     墨玉青进度条
│  └──────────────────────────────┘    │     $radius-md
│                                      │
│  ┌──────────────────────────────┐    │
│  │  ✓ 文本解析完成               │    │  ← 逐项完成提示，各 delay 0.3s
│  │  ☐ 说话人识别中...            │    │     已完成项: 墨玉青 ✓
│  │  ☐ 非对话内容过滤             │    │     进行中: 琥珀金呼吸
│  │  ☐ 特征提取准备               │    │     待处理: $ink-text-tertiary
│  └──────────────────────────────┘    │
│                                      │
└──────────────────────────────────────┘
```

**解析完成（SpeakerSelector）：**

```
┌──────────────────────────────────────┐
│  ← 返回     导入聊天记录               │
├──────────────────────────────────────┤
│                                      │
│  ┌──────────────────────────────┐    │
│  │  ✓  解析完成                  │    │  ← 成功 Banner
│  │  共识别 2 位说话人 · 97 条消息 │    │     bg: $ink-success-light
│  │  已自动过滤 8 条非对话内容      │    │     墨玉青色 ✓
│  └──────────────────────────────┘    │
│                                      │
│  请选择你的身份：                     │  ← $font-md 28rpx
│                                      │
│  ┌──────────────────────────────┐    │
│  │  ○  张三  发送 45 条          │    │  ← SpeakerSelector
│  │      最近发言: 昨天 14:32    │    │     选中态: 墨玉青圆圈
│  │      ┌─────────────────┐    │    │     按下: 背景 $ink-primary-light
│  │      │  预览消息片段     │    │    │     可展开预览 3 条示例消息
│  │      └─────────────────┘    │    │
│  └──────────────────────────────┘    │
│                                      │
│  ┌──────────────────────────────┐    │
│  │  ○  李四  发送 52 条          │    │
│  │      最近发言: 昨天 16:47    │    │
│  └──────────────────────────────┘    │
│                                      │
│  ┌──────────────────────────────┐    │
│  │       确认并开始学习          │    │  ← 墨玉青主按钮
│  └──────────────────────────────┘    │     disabled 时灰色
│                                      │
└──────────────────────────────────────┘
```

**错误与边界：**

| 场景 | 反馈 |
|------|------|
| 粘贴内容为空 | 按钮 disabled + 提示「请粘贴聊天记录」|
| 格式不兼容 | ErrorState 显示「无法识别该格式」+ 重试/重新粘贴按钮 |
| 消息超 3000 条 | 确认弹窗「建议导入最近 3000 条，是否继续？」|
| 仅识别到 1 位说话人 | 警告提示「仅识别到一人，建议检查粘贴内容」|
| 分析耗时 > 5s | ProgressBar 显示预估时间，避免用户焦虑 |

---

## 五、条件编译策略

### 5.1 平台差异色板

```scss
// 通过条件编译处理主题色
// App 端使用原生导航，小程序端使用自定义导航

// uni.scss
$primary-color: #07C160;      // 微信品牌绿
$bg-color-app: #f5f5f5;       // App 端背景
$bg-color-mp: #ededed;        // 小程序端背景
$bubble-self-bg: #95ec69;     // 自己气泡（浅绿）
$bubble-other-bg: #ffffff;    // 对方气泡（白色）
```

### 5.2 条件编译场景

| 差异点 | #ifdef APP-PLUS | #ifdef MP-WEIXIN |
|--------|-----------------|-----------------|
| 首页布局 | 两个入口卡片 | 两个入口卡片 + 空状态引导 |
| 导航栏 | 使用原生导航 | 自定义导航栏 |
| 页面注册 | 注册全部页面 | 排除 App 专用页面 |
| 导入流程 | 完整 6 步 | ✗ |
| 同步角色 | 创建时主动上传 | 进入时拉取 |
| 离线降级 | 支持小模型 | 提示需要网络 |
| 粘贴区域 | 全功能粘贴 | ✗ |
| 下载引导 | ✗ | 下载 App 引导页 |

### 5.3 条件编译示例

```vue
<!-- 首页组件示例 -->
<template>
  <PageContainer>
    <!-- App 端：版本号和隐私声明 -->
    <template #footer>
      <!-- #ifdef APP-PLUS -->
      <AppFooter />
      <!-- #endif -->
      <!-- #ifdef MP-WEIXIN -->
      <MiniProgramFooter />
      <!-- #endif -->
    </template>
  </PageContainer>
</template>
```

```typescript
// store/sync.ts
import { defineStore } from 'pinia';

export const useSyncStore = defineStore('sync', {
  actions: {
    async syncRoles() {
      // #ifdef APP-PLUS
      await this.uploadRoles();
      // #endif
      // #ifdef MP-WEIXIN
      await this.fetchRoles();
      // #endif
    }
  }
});
```

---

## 六、组件目录结构

```
components/
├── common/                      # 通用组件（双端共享）
│   ├── Avatar/
│   │   ├── Avatar.vue
│   │   └── Avatar.scss
│   ├── Loading/
│   │   └── Loading.vue
│   ├── EmptyState/
│   │   └── EmptyState.vue
│   ├── ErrorState/
│   │   └── ErrorState.vue
│   ├── NetworkAlert/
│   │   └── NetworkAlert.vue
│   ├── RoundCounter/
│   │   └── RoundCounter.vue
│   ├── StatusBadge/
│   │   └── StatusBadge.vue
│   ├── PageContainer/
│   │   └── PageContainer.vue
│   ├── ConfirmDialog/
│   │   └── ConfirmDialog.vue
│   └── ProgressBar/
│       └── ProgressBar.vue
│
├── business/                    # 业务组件
│   ├── ChatBubble/              # 聊天气泡（共用）
│   │   ├── ChatBubble.vue
│   │   └── ChatBubble.scss
│   ├── ChatMessageList/         # 消息列表（共用）
│   │   └── ChatMessageList.vue
│   ├── ChatInput/               # 输入框（共用）
│   │   └── ChatInput.vue
│   ├── RoleCard/                # 角色卡片（共用）
│   │   └── RoleCard.vue
│   ├── SessionCard/             # 会话卡片（共用）
│   │   └── SessionCard.vue
│   ├── StyleTag/                # 风格标签（共用）
│   │   └── StyleTag.vue
│   ├── SpeakerSelector/         # 说话人确认（App 端）
│   │   └── SpeakerSelector.vue
│   ├── ParseResultPreview/      # 解析结果预览（App 端）
│   │   └── ParseResultPreview.vue
│   ├── StyleReportCard/         # 风格报告单项（App 端）
│   │   └── StyleReportCard.vue
│   ├── ImportGuideStep/         # 导入引导步骤（App 端）
│   │   └── ImportGuideStep.vue
│   └── SyncStatusBar/           # 同步状态条（共用）
│       └── SyncStatusBar.vue
│
└── features/                    # 功能组件（页面级）
    ├── ImportGuide/             # App 端导入引导
    │   └── ImportGuide.vue
    ├── ImportPasteArea/         # App 端粘贴输入
    │   └── ImportPasteArea.vue
    ├── FeatureExtraction/       # App 端特征提取
    │   └── FeatureExtraction.vue
    ├── StyleReportBoard/        # App 端风格报告
    │   └── StyleReportBoard.vue
    └── DemoRoleChat/            # 小程序端示例角色
        └── DemoRoleChat.vue
```

---

## 七、页面目录结构

```
pages/
├── index/                       # 首页（双端，条件编译差异）
│   └── index.vue
│
├── chat/                        # 聊天相关
│   ├── chat.vue                 # 聊天界面（双端共用）
│   ├── sessions.vue             # 对话列表（双端共用）
│   └── history.vue              # 对话回顾（双端共用，只读模式）
│
├── role/                        # 角色相关
│   ├── create.vue               # 创建角色（App 端）
│   ├── import-guide.vue         # 导入引导（App 端）
│   ├── import-parse.vue         # 导入解析（App 端）
│   ├── style-report.vue         # 风格报告（App 端）
│   ├── detail.vue               # 角色详情（App 端）
│   └── list.vue                 # 角色列表（小程序端）
│
├── demo/                        # 示例体验
│   └── chat.vue                 # 示例角色对话（小程序端）
│
└── download/                    # 下载引导
    └── index.vue                # 下载 App 引导页（小程序端）
```

---

## 八、数据流与状态管理

### 8.1 核心数据流（App 端）

```
用户操作                  Store Action              本地存储         云端
─────────              ──────────────             ──────────      ────────
创建角色  ──→ roleStore.createRole()  ──→ SQLite.roles ──→ 同步云端
粘贴导入  ──→ importStore.parse()    ──→ (仅本地处理)
确认身份  ──→ importStore.confirmSpeaker()
开始学习  ──→ importStore.extract()  ──→ 生成特征文件
                                                    └──→ 特征同步云端
查看报告  ──→ importStore.getReport()
进入聊天  ──→ chatStore.startSession() ──→ SQLite.sessions
发送消息  ──→ chatStore.sendMessage() ──→ SQLite.messages
            └──→ API 云端生成
            　　  └──→ chatStore.appendReply()
                                                    └──→ 对话同步云端
```

### 8.2 核心数据流（小程序端）

```
用户操作                  Store Action              云端            本地缓存
─────────              ──────────────             ────────        ────────
打开首页  ──→ syncStore.fetchRoles()  ──→ 云端角色列表 ──→ 缓存角色
选择角色  ──→ chatStore.loadSession() ──→ 云端对话历史 ──→ 缓存对话
发送消息  ──→ chatStore.sendMessage()
            └──→ API 云端生成
            　　  └──→ chatStore.appendReply()  ──→ 缓存新消息
示例角色  ──→ chatStore.startDemo()    ──→ 预设数据
```

---

## 九、关键交互状态清单

### 9.1 每个页面覆盖的状态

| 页面 | 加载态 | 空状态 | 错误态 | 边界情况 |
|------|--------|--------|--------|----------|
| 首页 | App: Logo + 骨架屏载入 / 小程序: 自定义导航栏骨架 | 首次使用引导卡片 | 网络异常（小程度弹窗） | — |
| 聊天界面 | Skeleton 骨架屏（消息占位 3 条）+ NavBar 闪烁 | 新对话空消息 + 引导文案 | AI 回复失败（气泡抖动 + 重发） | 30 轮上限禁用输入 / 弱网自动降级 / 消息 > 200 条时分页加载 |
| 导入解析 | 逐项完成动画（每项 delay 0.3s） | — | 格式不兼容 ErrorState | > 3000 条确认弹窗 / 仅 1 说话人警告 |
| 风格报告 | 提取进度条 + 阶段文字说明 | — | 提取失败 + 重试按钮 | 对方消息 < 5 条提示精度不足 / 提取中途退出确认 |
| 角色列表 | 加载动画 + 灰色占位卡片 3 个 | 空角色引导「创建你的第一个角色」| 拉取失败 ErrorState | 离线模式显示本地缓存 |
| 对话回顾 | 加载中 Skeleton | 无历史对话 EmptyState | 加载失败 ErrorState | 超长对话分页加载（每页 50 条）|
| 示例对话 | — | — | — | 触发下载引导 / 30 轮上限 |
| 角色创建 | 头像上传进度 | — | 创建失败保留已填内容 | 重名提示 / 昵称超长截断 |
| 角色详情 | 角色信息加载 | — | 删除二次确认弹窗 | 删除后返回列表 / 重新导入确认覆盖 |

### 9.2 统一交互规范

**加载态优先序：** Skeleton（骨架屏）> Loading 组件（> 1s）> 局部 spinner（> 2s）

| 规范项 | 实现方式 | 细节参数 |
|--------|----------|----------|
| 骨架屏 | Skeleton 组件，灰色渐变块 | 动画: shimmer 1.5s infinite, bg: linear-gradient(90deg, $ink-border 25%, #f0efe9 50%, $ink-border 75%) |
| 空状态 | EmptyState 组件 | 置中图标 120rpx + 标题 28rpx + 描述 24rpx + 按钮 |
| 错误态 | ErrorState 组件 | 错误图标 + 红色提示 + 重试按钮（bg: $ink-primary）|
| 网络切换 | NetworkAlert 顶部滑入 | 0.3s ease-out, bg: $ink-warning-light, 文字: $ink-warning |
| 页面过渡 | uni-app 原生过渡动画 | 右滑入 0.3s（App）/ 淡入 0.2s（小程序）|
| 上拉加载 | 分页加载 + 底部加载指示器 | 每页 50 条, 触底前 100rpx 预加载 |

### 9.3 微交互设计规格

| 触发点 | 动效 | 时长 | 曲线 | 说明 |
|--------|------|------|------|------|
| 卡片按下 | scale(0.98) | 0.2s | ease | 所有可点击卡片统一按压反馈 |
| 气泡出现 | opacity 0→1 + translateY(20→0) | 0.3s | ease-out | 逐条消息自然涌现 |
| 发送失败 | translateX 抖动来回 3 次 | 0.4s | ease-in-out | 物理感反馈，替代纯红色警示 |
| 按钮按下 | scale(0.95→1) | 0.15s | ease | 主按钮触感反馈 |
| 列表滑动 | 惯性滚动 + 顶部弹性 | 原生 | — | App 端保持平台原声手感 |
| 输入框聚焦 | border-color 过渡 + box-shadow 出现 | 0.2s | ease | 墨玉青边框渐显 |
| 页面转场 | opacity + transform 联动 | 0.3s | ease-out | App: 右滑 / MP: 淡入 |
| 弹窗遮罩 | opacity 0→0.4 | 0.2s | ease | 半透明遮罩渐显 |
| 下拉刷新 | 墨玉青圆形指示器旋转 | 1s / cycle | linear | 跟随平台原生刷新规范 |
| 标签切换 | 下划线滑出 + 内容淡入 | 0.25s | ease | 下划线从当前滑向目标 |

### 9.4 禁用状态规范

| 元素 | 视觉表现 | 场景 |
|------|----------|------|
| 输入框 | bg: $ink-border + placeholder: $ink-text-tertiary | 30 轮上限 / 离线 |
| 发送按钮 | opacity: 0.4 + pointer-events: none | 输入为空 / 上限 |
| 主操作按钮 | bg: $ink-border + 文字: $ink-text-tertiary | 条件不满足 |
| 切换选项 | 文字 $ink-text-tertiary | 不可选区域 |
| 导航项 | 无跳转 + 轻微灰色 | 平台不可用功能 |

---

## 十、设计系统：「暖墨」(Warm Ink)

### 10.1 设计方向

```
品牌气质：温润 · 知性 · 有厚度
设计语言：暖纸为底、墨玉为骨、琥珀为缀

避免：冷白背景、紫色渐变、圆角泛滥、扁平无层次
追求：像一本旧书的翻页体验——纸有温度，墨有重量

差异化锚点：「装订线」体系——用左侧色条替代传统卡片边框，创造书籍装帧的阅读感
```

### 10.2 色彩体系

**三色各司其职：**

| 色彩 | Token | 色值 | 角色 | 使用场景 |
|------|-------|------|------|----------|
| 暖纸底 | `$ink-bg` | `#F7F5F0` | 画布底色 | 所有页面背景，区别于冷白 `#F5F5F5` |
| 墨玉青 | `$ink-primary` | `#2A9D8F` | 行为引导 | 主要按钮、自己气泡、输入态焦点、角色卡装订线 |
| 琥珀金 | `$ink-accent` | `#E9C46A` | 历史留痕 | 会话卡片装订线、勋章/标签、完成态 |
| 珊瑚橙 | `$ink-coral` | `#E76F51` | 警示 | 删除操作、高风险提示（慎用） |

**中性色阶：**

| Token | 色值 | 用途 |
|-------|------|------|
| `$ink-text` | `#1D1D1F` | 正文（不用纯黑，避免刺眼） |
| `$ink-text-secondary` | `#8E8E93` | 辅助文字（时间戳、描述） |
| `$ink-text-tertiary` | `#B8B8BD` | 禁用/占位文字 |
| `$ink-border` | `#E8E6E1` | 分割线、边框（偏暖，不冷灰） |
| `$ink-surface` | `#FFFFFF` | 卡片表面 |
| `$ink-primary-light` | `#E8F5F2` | 墨玉青淡色（按下态背景、标签底色） |

**语义色：**

| Token | 色值 | 场景 |
|-------|------|------|
| `$ink-error` / `$ink-error-light` | `#D15252` / `#FDF0F0` | 错误、失败 |
| `$ink-warning` / `$ink-warning-light` | `#D4A84B` / `#FDF6E8` | 警告 |
| `$ink-success` / `$ink-success-light` | `#2A9D8F` / `#E8F5F2` | 成功 |

**交互态色扩展：**

| Token | 色值 | 用途 |
|-------|------|------|
| `$ink-primary-dark` | `#21867A` | 墨玉青按下态/装订线加深 |
| `$ink-primary-disabled` | `#A3D0C9` | 墨玉青禁用态 |
| `$ink-primary-on` | `#FFFFFF` | 墨玉青背景上的文字色 |
| `$ink-surface-pressed` | `#F0EEE8` | 卡片按下背景 |
| `$ink-overlay` | `rgba(29,29,31,0.4)` | 弹窗遮罩层 |

**CSS 自定义属性映射：**

```scss
// 基础色板
--ink-bg: #F7F5F0;
--ink-primary: #2A9D8F;
--ink-primary-dark: #21867A;
--ink-primary-light: #E8F5F2;
--ink-primary-disabled: #A3D0C9;
--ink-accent: #E9C46A;
--ink-coral: #E76F51;
--ink-text: #1D1D1F;
--ink-text-secondary: #8E8E93;
--ink-text-tertiary: #B8B8BD;
--ink-border: #E8E6E1;
--ink-surface: #FFFFFF;
--ink-surface-pressed: #F0EEE8;
--ink-overlay: rgba(29, 29, 31, 0.4);

// 语义别名（便于语义化引用）
--ink-error: #D15252;
--ink-error-light: #FDF0F0;
--ink-warning: #D4A84B;
--ink-warning-light: #FDF6E8;
--ink-success: #2A9D8F;
--ink-success-light: #E8F5F2;

// 语义功能色
--color-bg: var(--ink-bg);
--color-surface: var(--ink-surface);
--color-primary: var(--ink-primary);
--color-text: var(--ink-text);
--color-text-secondary: var(--ink-text-secondary);
--color-border: var(--ink-border);
```

### 10.3 动效设计系统

```
原则：克制而有意义。每个动效都应有明确的「为什么」。
```

**动效 Token：**

| Token | 值 | 适用场景 |
|-------|-----|----------|
| `$motion-instant` | 0s | 即时反馈（toggle） |
| `$motion-fast` | 0.15s | 按钮按下、触感反馈 |
| `$motion-normal` | 0.25s | 焦点切换、标签切换 |
| `$motion-slow` | 0.3s | 消息出现、页面转场 |
| `$motion-slide` | 0.4s | 失败抖动、横幅滑入 |

| Token | 值 | 适用场景 |
|-------|-----|----------|
| `$ease-out` | cubic-bezier(0.16, 1, 0.3, 1) | 出现动画（弹性的舒适缓出） |
| `$ease-in-out` | cubic-bezier(0.65, 0, 0.35, 1) | 抖动、反馈 |
| `$ease` | cubic-bezier(0.25, 0.1, 0.25, 1) | 通用过渡 |

**动画一览：**

| 动效 | 时机 | 时长 | 曲线 | 延迟 | 原因 |
|------|------|------|------|------|------|
| 消息淡入 + 上移 | 新消息出现 | 0.3s | $ease-out | 逐条 +0.1s | 模拟真实对话逐条涌现 |
| 发送失败抖动 | 消息发送失败 | 0.4s | $ease-in-out | — | 物理反馈，比纯变色更直觉 |
| 卡片按下缩放 | 用户触摸卡片 | 0.2s | $ease | — | 物理按下感，模拟真实纸张 |
| 装订线变色 | 卡片按下时 | 0.2s | $ease | — | 指示当前交互焦点 |
| 网络横幅滑入 | 网络切换 | 0.3s | $ease-out | — | 不打断对话流的轻提示 |
| Typing 指示器 | AI 回复中 | 1.5s | 呼吸 | 三点分别 0/0.2/0.4s | 降低等待焦虑 |
| 弹窗遮罩渐显 | 弹窗打开 | 0.2s | $ease | — | 平滑焦点转移 |
| 骨架屏 shimmer | 内容加载 | 1.5s | linear | infinite | 占位反馈不突兀 |

**禁止：** 弹窗式动画干扰对话流、旋转加载超过 2 秒无反馈、无意义的闪烁/缩放。

**动画编排原则：**

```
1. 出现序列：页面进入 → 0ms NavBar → 100ms 内容区 → 200ms+ 消息逐条
2. 反馈优先：用户操作 → 50ms 内给出视觉反馈（按下态）
3. 异步提示：后台操作 → 静默完成不打扰，失败时提示
4. 减少动效：低性能模式 / 辅助模式 → 关闭非必要动画
```

### 10.4 空间与排版

**布局原则：**

```
装订线（Binding Line）：卡片左侧 6rpx 色条
  - 角色卡片 → 墨玉青（主力身份）
  - 会话卡片 → 琥珀金（历史痕迹）
  - 功能入口 → 按下时弹出墨玉青

留白节奏（8 点网格）：
  - 页面两侧 30rpx（~4格）
  - 卡片内边距 24rpx（3格）
  - 卡片间距 16rpx（2格）
  - 元素间距 12rpx（1.5格）
  - 行内间距 8rpx（1格）
```

**字号阶梯：**

```scss
// 以 rpx 为单位，保持跨屏一致性
$font-xs:  22rpx;  // 时间戳、辅助文字
$font-sm:  24rpx;  // 描述、元信息
$font-base: 26rpx; // 正文
$font-md:  28rpx;  // 卡片标题
$font-lg:  32rpx;  // 小标题
$font-xl:  36rpx;  // 大标题
$font-xxl: 48rpx;  // 页面标题

// 行高
$line-tight: 1.2;   // 标题
$line-normal: 1.4;  // 正文
$line-loose: 1.6;   // 长文
```

**圆角系统：**

```scss
$radius-sm:  8rpx;      // 小元素（标签、徽章）
$radius-md:  12rpx;     // 卡片、按钮、输入框
$radius-lg:  20rpx;     // 气泡、大卡片
$radius-xl:  28rpx;     // Modal、底部弹窗
$radius-full: 9999rpx;  // 胶囊标签、圆形头像

// 气泡圆角不对称设计（模拟对话气泡的有机感）
// 自己气泡：右上 $radius-sm + 其余 $radius-lg
// 对方气泡：左上 $radius-sm + 其余 $radius-lg
```

**阴影层级：**

```scss
// Y 偏移, 模糊, 扩展, 颜色 (偏暖阴影)
$shadow-sm:  0 2rpx 8rpx rgba(29, 29, 31, 0.06);   // 卡片默认
$shadow-md:  0 4rpx 20rpx rgba(29, 29, 31, 0.08);  // 浮动面板
$shadow-lg:  0 8rpx 40rpx rgba(29, 29, 31, 0.12);  // 弹窗/Modal

// 注意：阴影使用偏暖的黑色（29,29,31）而非纯黑(0,0,0)，与暖色调背景协调
```

### 10.5 字体

```scss
// 跟随系统字体，不对抗平台习惯
font-family: -apple-system, BlinkMacSystemFont,
             'Helvetica Neue', 'PingFang SC',
             'Microsoft YaHei', sans-serif;

// 数字/英文等宽（时间戳、计数场景）
// 使用系统等宽字体
$font-mono: ui-monospace, 'SF Mono', 'Menlo', monospace;
```

注：移动端不引入自定义字体（包体积 + 渲染性能），利用系统字体的平台熟悉感。

### 10.6 组件视觉语言速查

| 组件 | 视觉特征 | 尺寸 | 交互反馈 | 状态 |
|------|----------|------|----------|------|
| `ChatBubble`（自己） | 墨玉青 `$ink-primary` 背景、右上 `$radius-sm` 其余 `$radius-lg`、`shadow-sm` | max 580rpx | sending: 右下角 8rpx 旋转圈 / failed: 抖动 0.4s + 红色!号 | sending / sent / failed |
| `ChatBubble`（对方） | 白底 `$ink-surface`、左上 `$radius-sm` 其余 `$radius-lg`、`shadow-sm` | max 580rpx | — | default |
| `RoleCard` | 左墨玉青装订线 6rpx、`shadow-sm`、`$radius-md` | h: 160rpx | 按下 `scale(0.98)` 0.2s + 装订线 `$ink-primary-dark` | default / pressed |
| `SessionCard` | 左琥珀金装订线 6rpx、`shadow-sm`、`$radius-md` | h: 140rpx | 按下 `scale(0.98)` 0.2s + bg `$ink-surface-pressed` | default / pressed / active |
| `ChatInput` | 白底、`$radius-md`、`$ink-border` 边框 | h: 72rpx（单行） | focus: 墨玉青边框 2rpx + box-shadow / disabled: bg `$ink-border` | default / focus / disabled |
| `EmptyState` | 居中布局、图标 120rpx、标题 `$font-md`、描述 `$font-sm` | 占满容器 | 按钮按下 scale(0.95) | — |
| `ErrorState` | 错误图标 + 红色提示 + 重试按钮 | 占满容器 | 重试按钮墨玉青 | — |
| `ProgressBar` | 墨玉青进度条 `$radius-md`、百分比文字 `$font-sm` | h: 12rpx | 进度颜色过渡 0.3s | normal / warning / complete |
| `StyleTag` | 圆角胶囊 `$radius-full`、语义色背景、内边距 16rpx/8rpx | auto | — | 多种语义色变体 |
| `NetworkAlert` | 顶部横幅 `$ink-warning-light` 背景、`$ink-warning` 文字 | h: 60rpx | 滑入 0.3s / 滑出 0.3s | visible / hidden |
| `ConfirmDialog` | 居中 Modal、`$radius-xl`、遮罩 `$ink-overlay` | w: 600rpx | 遮罩渐显 0.2s + 弹窗 scale(0.9→1) 0.25s | — |
| `SyncStatusBar` | 单行条、状态指示圆点 + 文字 | h: 48rpx | 状态切换淡入 0.2s | syncing / online / offline / error |
| `RoundCounter` | 进度条 + 数字 + 状态文字 | h: 36rpx | 25+ 琥珀金渐入 / 30 珊瑚橙 + 脉冲 | normal / warning / reached |
| `Avatar` | 圆形 `$radius-full`、`shadow-sm` | 64rpx / 80rpx / 96rpx | 加载中 shimmer / 加载失败 fallback 首字 | loading / loaded / error |

### 10.7 CSS 变量架构

**三层变量体系：**

```scss
// ── Layer 1: 基础 Token（不可变） ──
// _tokens.scss
:root {
  // 色彩
  --ink-bg: #F7F5F0;
  --ink-primary: #2A9D8F;
  --ink-accent: #E9C46A;
  --ink-coral: #E76F51;
  // ...（完整色板见 10.2）

  // 空间
  --space-page: 30rpx;
  --space-card-padding: 24rpx;
  --space-card-gap: 16rpx;
  --space-element-gap: 12rpx;
  --space-inline-gap: 8rpx;

  // 圆角
  --radius-sm: 8rpx;
  --radius-md: 12rpx;
  --radius-lg: 20rpx;
  --radius-xl: 28rpx;
  --radius-full: 9999rpx;

  // 阴影
  --shadow-sm: 0 2rpx 8rpx rgba(29, 29, 31, 0.06);
  --shadow-md: 0 4rpx 20rpx rgba(29, 29, 31, 0.08);
  --shadow-lg: 0 8rpx 40rpx rgba(29, 29, 31, 0.12);

  // 动效
  --motion-fast: 0.15s;
  --motion-normal: 0.25s;
  --motion-slow: 0.3s;
  --motion-slide: 0.4s;
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);

  // 字体
  --font-xs: 22rpx;
  --font-sm: 24rpx;
  --font-base: 26rpx;
  --font-md: 28rpx;
  --font-lg: 32rpx;
  --font-xl: 36rpx;
  --font-xxl: 48rpx;
}

// ── Layer 2: 语义 Token（组件消费） ──
// _semantic.scss
:root {
  --color-bg: var(--ink-bg);
  --color-surface: var(--ink-surface);
  --color-primary: var(--ink-primary);
  --color-primary-dark: var(--ink-primary-dark);
  --color-primary-light: var(--ink-primary-light);
  --color-primary-disabled: var(--ink-primary-disabled);
  --color-text: var(--ink-text);
  --color-text-secondary: var(--ink-text-secondary);
  --color-text-tertiary: var(--ink-text-tertiary);
  --color-border: var(--ink-border);
  --color-error: var(--ink-error);
  --color-warning: var(--ink-warning);
  --color-success: var(--ink-success);
}

// ── Layer 3: 组件 Token（组件内部） ──
// 在每个组件 SCSS 中定义，引用语义 Token
// 示例：ChatBubble
.chat-bubble--self {
  --bubble-bg: var(--color-primary);
  --bubble-text: #FFFFFF;
  --bubble-radius: var(--radius-lg);
  --bubble-radius-tail: var(--radius-sm);
}
```

**使用原则：**
- 组件内优先使用语义 Token（Layer 2），不直接引用基础色值
- 组件变量（Layer 3）以 `--bubble-*`、`--card-*`、`--input-*` 命名
- 主题切换时只需覆盖 Layer 1 基础色值，Layer 2/3 自动响应

### 10.8 深色模式适配蓝图

```scss
// 深色模式色板映射（准备方案）
[data-theme="dark"] {
  // 基础色反转
  --ink-bg: #1C1C1E;
  --ink-surface: #2C2C2E;
  --ink-border: #38383A;
  --ink-text: #F5F5F7;
  --ink-text-secondary: #98989D;
  --ink-text-tertiary: #636366;

  // 品牌色微调（深色背景下降低明度，保持对比度）
  --ink-primary: #3AAFA0;
  --ink-primary-light: #2A4A46;
  --ink-primary-dark: #4DBDB0;

  // 阴影调整为发光效果
  --shadow-sm: 0 2rpx 8rpx rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4rpx 20rpx rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 8rpx 40rpx rgba(0, 0, 0, 0.5);

  // 气泡调整
  --bubble-self-bg: var(--ink-primary);
  --bubble-other-bg: #3A3A3C;
}
```

注：深色模式暂不实现 MVP，保留变量结构便于后续切换。检测策略使用 `prefers-color-scheme` + 手动切换覆盖。

### 10.9 无障碍要点

| 规范 | 要求 | 检查方式 |
|------|------|----------|
| 对比度 | 正文 ≥ 4.5:1, 大标题 ≥ 3:1 | 暖纸底 + 墨玉青 6.2:1 ✓ |
| 触控区域 | 最小 44×44pt（约 88×88rpx） | 所有按钮和卡片需达标 |
| 焦点指示 | 键盘模式显示清晰焦点环 | 墨玉青 2rpx outline |
| 动画减少 | `prefers-reduced-motion` 关闭非必要动画 | CSS media query 降级 |
| 字体缩放 | 支持系统字体大小调整 | 避免固定 px 值，使用 rpx / em |

---

## 十一、附录

### 11.1 参考文档

- 产品方案：`产品文档.md`
- 技术方案：`项目文档.md`
- 开发规范：`CLAUDE.md`
- 设计 Token：`src/components/common/_variables.scss`

### 11.2 待补充

- [ ] 各页面详细 UI 设计稿（Figma / Sketch）
- [ ] 深色模式适配方案
- [ ] 无障碍（Accessibility）规范
- [ ] 截屏/录制用于应用商店展示
