# 部署指南

## 概述

本应用基于 uni-app (Vue 3) + uniCloud-aliyun，分为 App端（iOS/Android）、微信小程序端。

---

## 一、云数据库部署

### 1.1 创建集合

在 uniCloud 控制台（或通过 HBuilderX → 云开发 → 数据库）创建以下 4 个集合：

| 集合名 | 说明 | 索引 |
|--------|------|------|
| `cloud_roles` | 角色数据 | `user_id`, `user_id+name`(唯一), `updated_at` |
| `cloud_sessions` | 会话数据 | `role_id`, `user_id`, `updated_at`, `role_id+updated_at` |
| `cloud_messages` | 消息记录 | `session_id`, `session_id+created_at`, `role_id` |
| `rate_limits` | 频率限制 | `user_id+date`(唯一) |

可以通过以下方式创建：

**方式 A — HBuilderX 可视化操作：**
1. 打开项目 → 顶部菜单「云开发」→ 「云数据库」
2. 分别创建以上集合
3. 在每个集合的「索引」选项卡中添加对应索引

**方式 B — schema 文件上传：**
`src/uniCloud-aliyun/database/` 目录下已包含 `.schema.json` 文件，在 HBuilderX 中右键 → 「上传 DB schema」即可自动创建集合和索引。

### 1.2 设置权限

每个集合的权限已在 schema 文件中配置（基于 `auth.uid` 的用户级读写），上传 schema 后自动生效。

---

## 二、云函数部署

### 2.1 上传云函数

两个云函数位于 `src/uniCloud-aliyun/cloudfunctions/`：

| 云函数 | 说明 |
|--------|------|
| `sync-api` | 跨端同步 API（角色/会话/消息 CRUD） |
| `chat-generate` | AI 回复生成（调用 LLM API） |

**部署步骤：**
1. 在 HBuilderX 中打开项目
2. 右键 `src/uniCloud-aliyun/cloudfunctions/sync-api` → 「上传部署」
3. 右键 `src/uniCloud-aliyun/cloudfunctions/chat-generate` → 「上传部署」

### 2.2 配置环境变量

chat-generate 云函数需要配置环境变量：

在 uniCloud 控制台 → 云函数 → chat-generate → 配置，添加以下变量：

| 变量名 | 必填 | 说明 | 默认值 |
|--------|------|------|--------|
| `LLM_API_KEY` | 生产环境必填 | OpenAI 兼容 API 密钥 | 空（使用本地模板） |
| `LLM_BASE_URL` | 否 | API 基础地址 | `https://api.openai.com/v1` |
| `LLM_MODEL` | 否 | 模型名称 | `gpt-4o-mini` |
| `RATE_LIMIT_DAILY` | 否 | 单用户每日调用上限 | `100` |

> **开发环境**：不配置 `LLM_API_KEY` 时，云函数会自动使用本地模板回复（基于随机模板 + 高频词插入），适合功能开发和 UI 调试。

---

## 三、配置检查清单

### 3.1 manifest.json 检查

| 配置项 | 说明 |
|--------|------|
| `uniCloud.spaceId` | 确认与 uniCloud 服务空间 ID 一致 |
| `mp-weixin.appid` | 填写微信小程序 AppID |
| `app-plus.distribute` | App 图标、应用名称、版本号 |

### 3.2 首次启动验证

1. **清除缓存**：删除小程序/App 数据或使用 `uni.clearStorage()` 重置
2. **验证流程**：
   - 启动 → 显示隐私政策页面
   - 勾选「我已阅读并同意」→ 点击「确认并继续」
   - 跳转到首页 → 正常展示
3. **二次启动**：清除进程后重新打开 → 直接进入首页（不重复展示隐私页）

### 3.3 云函数验证

**sync-api 测试：**
```js
// 在 HBuilderX 控制台或 uni-app 中调用
uniCloud.callFunction({
  name: 'sync-api',
  data: { action: 'getRoles', user_id: 'test_user_001' }
}).then(res => console.log(res))
```
预期返回 `{ code: 200, data: { roles: [] } }`

**chat-generate 测试：**
```js
uniCloud.callFunction({
  name: 'chat-generate',
  data: {
    message: '你好',
    target_speaker: '小明',
    session_id: 'test_session',
    user_id: 'test_user_001'
  }
}).then(res => console.log(res))
```
预期返回 `{ code: 200, data: { reply: '...', message_id: '...' } }`

---

## 四、部署流程（建议顺序）

```
  ① 创建云数据库集合
      ↓
  ② 上传云函数
      ↓
  ③ 配置环境变量
      ↓
  ④ 配置 manifest.json
      ↓
  ⑤ 运行验证
```

---

## 五、常见问题

### Q: 云函数返回 `{ code: 500, error: '集合不存在' }`
A: 未创建对应数据库集合。检查 uniCloud 控制台 → 数据库，确认所有集合已创建。

### Q: 云函数上传失败
A: 检查 uniCloud 服务空间是否绑定正确，以及网络连接。

### Q: 小程序真机调试白屏
A: 检查微信开发者工具 → 详情 → 本地设置 → 勾选「不校验合法域名」，或在微信公众平台配置 request 合法域名。

### Q: 隐私政策页面重复出现
A: 手动执行 `uni.setStorageSync('privacy_consented', 'true')` 跳过，或检查 `uni.getStorageSync('privacy_consented')` 返回值。
