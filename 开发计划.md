# 开发计划（v1.0 MVP）

> 基于产品文档、项目文档、页面结构与组件文档制定。最终版。

---

## 一、核心依赖关系

### 关键路径

```
第一周可用 ──────────────────────────────────────────
  Foundation（uni-app + 云函数）
      │
      ├── UI 组件库（可独立进行）
      │       │
      │       ├── 聊天组件 ←──── 云端 AI API（依赖）
      │       └── 业务页面 ←──── 解析引擎（依赖）
      │
      ├── 解析引擎 → 特征提取 → 风格报告（App 核心链路）
      │
      └── 云数据库 → 同步 API → 小程序 UI（跨端链路）
```

### 并行策略

```
Track A（App 端核心）        Track B（云端 + 小程序）      Track C（UI/UX）
────────────────────         ──────────────────────       ─────────────────
解析引擎                     云数据库设计                   通用组件库
特征提取（JS）               云端 AI 转发 API              聊天气泡组件
导入流程 UI                  跨端同步 API                  页面 UI 实现
聊天界面集成                 小程序页面                   动效 + 状态
```

---

## 二、阶段规划

### 阶段 0：技术验证（1-2 周）

**目标：** 在正式开发前消除关键技术风险。

| 验证项 | 验收标准 | 风险等级 |
|--------|----------|----------|
| 微信聊天记录多格式解析准确率 | 收集 50+ 真实样本，准确率 > 95% | 高 |
| 云端大模型风格模仿效果 | 盲测 60%+ 无法辨认真伪 | 高 |
| 纯 JS 风格特征提取效果 | 能准确提取词频、口头禅、语气特征 | 中 |
| 云端 API 成本估算 | 确认每日 100 次/用户的成本范围，不超预算红线 | 高 |
| uni-app + 条件编译框架验证 | App 端和小程序端能正常编译运行 | 低 |
| SQLite 在 uni-app 中的可用性 | 基础 CRUD 通过 | 低 |
| 端侧小模型推理速度（降级用） | 中端机 < 5s（离线降级非核心场景） | 低 |

**决策门：** 三项高风险验证全部通过，否则重新评估方案。

---

### 阶段 1：基础框架 + UI 组件库（2 周）

**目标：** 搭好双端运行框架，完成可复用的 UI 组件库。

#### 1A：项目基础设施（并行）

```
[1-3 天]
□ uni-app 项目初始化（Vue 3 + Pinia + SCSS）
□ pages.json 路由注册（全量页面）
□ 条件编译目录结构搭建
□ 样式变量体系（uni.scss）
□ Git 仓库初始化和分支策略

[3-5 天]
□ 云函数项目初始化
□ 云数据库表结构创建（roles / sessions / messages）
□ 用户登录与标识体系（App 端 + 小程序端统一 user_id）
□ 网络层封装（uni.request + 错误处理 + token 管理）
□ SQLite 数据库初始化与 CRUD 封装
```

#### 1B：UI 组件库（并行，与 1A 同步）

```
[5-8 天]
□ 通用组件层
  ├── Avatar（头像，含加载/错误态）
  ├── Loading（骨架屏优先，spinner 兜底）
  ├── EmptyState（空状态：插图 + 文案 + 按钮）
  ├── ErrorState（错误态：文案 + 重试按钮）
  ├── NetworkAlert（网络状态横幅）
  ├── RoundCounter（轮数计数器：green → yellow → red）
  ├── StatusBadge（同步/在线/离线状态标识）
  ├── PageContainer（页面容器：导航栏 + 安全区 + 页脚）
  ├── ConfirmDialog（确认弹窗）
  └── ProgressBar（进度指示条）

□ 业务组件层
  ├── ChatBubble（聊天气泡：绿/白 + 头像 + 时间戳）
  ├── ChatMessageList（消息列表：自动滚动 + typing + 分页）
  ├── ChatInput（输入框：placeholder + 发送按钮 + 禁用态）
  ├── RoleCard（角色卡片：头像 + 昵称 + 简介）
  ├── SessionCard（会话卡片：标题 + 轮数 + 时间 + 状态）
  ├── StyleTag（风格标签：色块 + 文案）
  ├── SpeakerSelector（说话人确认：单选卡片列表）
  ├── ParseResultPreview（解析预览：统计摘要）
  └── StyleReportCard（风格报告单项：维度 + 值 + 图标）
```

**里程碑：** 组件库验收。所有组件在 HBuilder X 可视化预览中通过。EmptyState、ErrorState、Loading 三大通用态全部覆盖。

---

### 阶段 2：App 端核心链路（3 周）

**目标：** 跑通"导入 → 解析 → 提取 → 对话"完整流程。

#### 2A：解析引擎（纯 JS，与 2B 并行）

```
[3-5 天]
□ 实现格式检测器（iOS / Android / PC 三种格式自动识别）
□ 实现消息解析器（正则拆分、说话人提取、时间戳解析）
□ 实现过滤引擎（系统消息 / 红包 / 转账 / 图片 / 语音过滤）
□ 实现说话人去重和统计模块
□ 单元测试覆盖：50+ 真实样本
```

#### 2B：导入流程 UI（与 2A 并行）

```
[3-5 天]
□ 导入引导页（ImportGuide）：分平台导出教程 + 示例图
□ 导入粘贴页（ImportPasteArea）：粘贴区域 + 格式示例折叠
□ 解析结果预览页：统计摘要 + SpeakerSelector 整合
□ 特征提取进度页：ProgressBar + 动画
□ 风格分析报告页：StyleReportBoard + StyleTag 展示
```

#### 2C：特征提取模块（2A 完成后）

```
[2-3 天]
□ 词频统计器（高频词 + 停用词过滤）
□ 口头禅提取器（N-gram 高频短语）
□ 语气分析器（语气词频率、口语化程度、标点习惯）
□ 表情分析器（emoji 频率和偏好统计）
□ 回复长度分布（平均字数、极值、分布）
□ 风格特征文本生成器（输出结构化的特征描述）
```

#### 2D：聊天核心（2B 完成后，与 2C 并行）

```
[5-7 天]
□ 聊天界面布局（NavBar + MessageList + Input）
□ ChatMessageList + ChatBubble + ChatInput 整合
□ 云端 AI API 对接（请求/响应/错误处理）
□ Tyiping 指示器动画
□ 对话轮数计数 + 30 轮上限逻辑
□ 达到上限后的三个操作按钮（开启新对话 / 查看历史 / 返回角色页）
□ 本地对话持久化（SQLite）
□ 对话列表页
□ 对话回顾页（只读模式）
```

#### 2E：角色管理（分散集成）

```
[2-3 天]
□ 角色创建页（昵称 + 头像选择器）
□ 角色详情页（角色信息 + 操作入口）
□ 重新导入聊天记录逻辑
□ 删除角色逻辑（含 SQLite + 云端级联删除）
```

**里程碑：** App 端 Alpha。可在真机上完成"创建角色 → 导入 → 解析 → 特征提取 → 聊天"完整流程。

---

### 阶段 3：云端 + 小程序端（2 周）

**目标：** 小程序端上线，跨端同步跑通。

#### 3A：云端服务（与 3B 并行）

```
[5-7 天]
□ 云端 AI 生成 API（转发层 + prompt 工程 + 上下文管理）
  ├── 请求结构：role + style_features + context + message + session_id
  ├── 响应结构：reply + message_id + tokens_used
  ├── 错误码：200 / 429 / 500 / 503
  └── 限流策略：单用户 100 次/天，10 次/分钟
□ 跨端同步 API
  ├── 上传角色（POST /roles）
  ├── 获取角色列表（GET /roles）
  ├── 同步对话记录（POST /sessions/:id/messages）
  └── 获取对话历史（GET /sessions/:id/messages）
□ 云端对话持久化 + 限流计数器
□ API 监控与日志
```

#### 3B：小程序端 UI（与 3A 并行）

```
[5-7 天]
□ 小程序首页（条件编译：示例角色 + 角色列表入口）
□ 示例角色对话页（预设角色 + 完整聊天体验）
□ 角色列表页（云端同步 + 空状态引导下载 App）
□ 聊天界面（复用 ChatBubble/ChatInput/ChatMessageList）
□ 对话回顾页（复用只读模式）
□ 对话轮数计数器与上限提示
□ 下载 App 引导页
□ 条件编译保证包体积不超限
```

#### 3C：跨端同步集成（3A 完成后）

```
[2-3 天]
□ App 端上传逻辑（创建角色/对话后增量同步）
□ 小程序端拉取逻辑（打开列表/进入对话时拉取）
□ 同步冲突处理（以 App 端为准）
□ 同步状态指示（SyncStatusBar）
□ 用户标识统一（跨端关联）
```

**里程碑：** Beta 版。App 端和小程序端数据互通，双端均可对话。

---

### 阶段 4：UI 打磨 + 全量状态覆盖（1 周）

**目标：** 所有页面覆盖加载态 / 空状态 / 错误态 / 边界情况。

| 页面 | 需覆盖的状态 |
|------|-------------|
| 首页（App） | 首次使用引导 |
| 首页（小程序） | 首次使用引导、空角色引导下载 |
| 聊天界面 | 消息加载中、AI 回复失败、网络切换离线、30 轮上限 |
| 导入引导 | ✗（静态页） |
| 导入粘贴 | 粘贴格式不兼容提示 |
| 导入解析 | 解析中动画、格式错误、超过 3000 条提示 |
| 特征提取 | 提取进度、提取失败、消息少于 5 条提示精度不足 |
| 风格报告 | 展示完成（无错误态） |
| 角色列表 | 加载中、空状态、加载失败 |
| 对话列表 | 加载中、空状态 |
| 对话回顾 | 加载中、空状态、加载失败 |
| 角色详情 | ✗（静态信息页） |
| 示例对话 | 体验后引导下载 |

```
[5 天]
□ 各页面骨架屏实现
□ 空状态覆盖（插画 + 文案 + 操作按钮）
□ 错误态覆盖（文案 + 重试 + 返回操作）
□ 网络切换提示动画
□ 页面过渡动画
□ 消息发送/接收微动效
□ 输入框聚焦/失焦体验优化
□ 键盘弹出/收回适配（App 端）
```

---

### 阶段 5：测试 + 修复（1.5 周）

| 测试类型 | 内容 | 工具/方法 |
|----------|------|----------|
| 单元测试 | 解析引擎、特征提取、过滤规则 | 本地 Jest / 手动 |
| 集成测试 | 导入 → 提取 → 对话 → 同步 全链路 | 真机实测 |
| 兼容性测试（App） | iOS 主流机型 + Android 主流机型（各 5+ 机型） | 云真机 / 实机 |
| 兼容性测试（小程序） | 各版本微信兼容 | 小程序开发者工具 |
| 性能测试 | AI 回复速度 < 3s、特征提取 < 2s | 真机计时 |
| 弱网测试 | 3G/弱 Wi-Fi/断网 三种场景 | 弱网工具 |
| 跨端同步测试 | App 创建 → 小程序查看 → 继续对话 | 双端配合 |
| 用户测试 | 3-5 人内测 | TestFlight / 小程序体验版 |

---

### 阶段 6：上线准备（1 周）

```
□ 隐私政策与用户协议定稿（三层数据策略）
□ App Store 审核材料（截图 + 描述 + 隐私链接）
□ Android 应用商店审核材料
□ 微信小程序审核材料准备
□ 云服务生产环境部署（正式域名 + SSL + 监控）
□ 成本监控面板搭建
□ 灰度发布方案
└── 正式上架
```

---

## 三、工作量与排期

### 乐观估算（1 人全栈）

```
阶段 0：技术验证          1-2 周
阶段 1：基础框架+组件     2 周
阶段 2：App 核心链路       3 周
阶段 3：云端+小程序        2 周
阶段 4：UI 打磨            1 周
阶段 5：测试               1.5 周
阶段 6：上线准备           1 周
─────────────────────────────
总计：11.5 - 12.5 周 ≈ 3 个月
```

### 建议分工（2-3 人）

| 角色 | 人数 | 负责内容 |
|------|------|----------|
| 前端（uni-app） | 1 | Track A + C：App 端 UI、小程序端 UI、组件库 |
| 后端（云函数） | 1 | Track B：云数据库、AI 转发 API、跨端同步 API |
| 全栈（兜底） | 1 | 解析引擎、特征提取、Prompt 工程、测试统筹 |

---

## 四、风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 云端大模型风格模仿效果不达标 | 中 | 高 | 阶段 0 必须通过盲测；预留多个模型备选 |
| 聊天记录格式解析覆盖不全 | 中 | 高 | 阶段 0 收集 50+ 样本；上线后加用户反馈通道 |
| 云端 API 成本超预算 | 中 | 中 | 阶段 0 确认成本；100 次/天的上限是安全阀 |
| 小程序审核被拒 | 低 | 中 | 不涉及创建/导入敏感功能；准备人审材料 |
| App Store 审核被拒 | 低 | 中 | 隐私政策三层数据策略写清楚 |
| 端侧模型集成失败（降级用） | 低 | 低 | 降级走在线优先方案，端侧模型降级不影响核心功能 |

---

## 五、交付物清单

| 里程碑 | 交付物 | 验收人 |
|--------|--------|--------|
| 技术验证完成 | 验证报告（解析准确率 / 风格盲测 / 成本估算） | PM |
| 组件库完成 | HBuilder X 组件预览工程 | PM + UI |
| App 端 Alpha | 导入→提取→对话 全链路可用 | PM + QA |
| Beta 版 | App + 小程序 双端可用，数据互通 | PM + QA |
| 测试完成 | 测试报告 | PM + QA |
| 正式上线 | App Store + Android 商店 + 微信小程序 | PM |
