产品方案：基于微信聊天记录的 AI 模拟对话（v1.0 正式版·修订）

一、产品定位

一款帮助用户与"AI 模拟的好友"聊天的工具。AI 通过学习用户提供的真实微信聊天记录，模仿对方的说话风格和语言习惯，实现趣味性的风格模拟对话体验。

定位说明：本产品模拟的是"说话风格"而非"人格或记忆"。AI 可以学习对方爱用的词汇、语气、表情符号和回复节奏，但无法复刻对方的记忆、思维或情感。请将其视为一个风格模仿工具，而非真正的"数字分身"。

二、目标用户

- 怀念与某位好友聊天感觉的人（如异地、疏远的朋友）
- 想练习与特定对象对话的人
- 出于好奇或情感陪伴需求的人

注意：不对"逝者模拟"场景做任何主动宣传或引导，产品定位为"风格模仿工具"，用户自发使用场景由用户自行负责。

三、核心功能（MVP）

| 模块 | 功能 | 实现方式 | MVP 限制 |
|------|------|----------|----------|
| 聊天记录导入 | 手动粘贴导入 | 用户从微信复制完整聊天记录，粘贴到 App；自动识别 iOS/Android/PC 格式，过滤非对话内容（系统消息、红包等） | 单次最多导入 3000 条消息 |
| AI 学习 | 提取对方说话风格 | 本地处理，自动识别对方消息，提取语癖、常用词、回复长度、表情使用等特征 | 学习完成后展示风格分析报告 |
| 模拟对话 | 与 AI 聊天 | 用户发送消息，AI 以对方身份和风格回复（云端生成，基于本地提取的风格特征） | 单次对话上限 30 轮 |
| 聊天界面 | 微信风格 UI | 绿气泡、白气泡、头像、昵称，还原对话感 | - |
| 对话历史 | 本地保存 | 所有模拟对话存储在设备本地 | - |
| 示例体验 | 预设角色试用 | 提供 1 个内置示例角色，用户无需导入即可体验对话效果 | 示例角色为虚构人物 |

> **平台差异说明：** 微信小程序轻量版不支持「导入聊天记录」和「创建新角色」等本地重型操作（需在 App 端完成），但示例角色体验和已有角色的对话生成（含新对话）均通过云端 API，能力与 App 端一致。创建的角色和对话记录自动同步至各端。

四、已确认的决策（修订）

| 决策项 | 选择 | 备注 |
|--------|------|------|
| 记录导入方式 | 手动粘贴导入（后续扩展 OCR 和系统分享） | MVP 仅支持手动粘贴导入；截图识别（OCR）和系统分享导入后续迭代 |
| AI 方案 | 本地分析 + 云端生成（混合模式） | 本地提取风格特征（原始数据不上云），云端大模型按特征生成回复；同时保留本地降级能力 |
| 隐私策略 | 原始数据不上云，按需持久化 | ①原始聊天记录永不上云；②风格特征云端持久化（不可逆，无法还原原始对话）；③对话历史云端持久化（用于跨端同步查看和继续对话） |
| 角色管理 | 单角色 | MVP 仅模拟一个人，后续支持多角色 |
| 平台策略 | MVP：App 端（完整功能）+ 微信小程序（轻量版） | App 端提供完整功能（导入+创建角色+对话）；小程序轻量版仅限示例角色体验和已有对话回顾，不支持创建新角色；H5 规划中 |
| 跨端同步 | 云端持久化风格特征和对话历史 | App 创建的角色和对话记录同步至云端，小程序直接读取；原始聊天记录不上云 |
| 聊天记录格式兼容 | 适配 iOS/Android/PC 三种格式 | 格式不一致直接导致解析失败或错乱，需在导入流程中自动识别格式并统一处理 |
| 收费模式 | 免费（有限制） | MVP 免费使用，导入消息上限 3000 条、对话上限 30 轮/次；后续迭代提供付费解锁更多用量和功能 |
| 技术验证 | 开发前验证 | 先跑通"本地特征提取 → 云端生成"核心链路，确认风格模仿效果可接受后进入正式开发 |

五、用户操作流程（修订）

打开 App
  → 首页：可选择 [体验示例角色] 或 [创建新角色]
      │
      ├─ [体验示例角色]：直接进入聊天界面，与预设AI角色对话（8轮免费体验后引导创建真实角色）
      │
      └─ [创建新角色]
          → 设置对方昵称、头像
          → 显示导入引导
              ├─ 分平台说明如何从微信导出聊天记录（iOS/Android/PC 截图 + 步骤）
              ├─ 提示：支持 iOS、Android、PC 端微信复制出的格式，系统消息/红包/图片会自动过滤
              └─ 示例：粘贴后展示一段格式样例帮助用户理解

          → 粘贴聊天记录，点击"开始解析"

          → 解析过程（本地，< 1 秒）
              ├─ 自动识别格式（iOS/Android/PC）
              ├─ 自动过滤非对话内容（系统消息、红包、转账、小程序卡片、图片、语音）
              ├─ 自动分割对话轮次
              └─ 展示解析结果："识别到 2 位说话人，共 XX 条消息（已过滤 X 条非对话内容）"

          → 用户确认身份
              ├─ "请选择您是谁"（列出解析到的说话人列表 + 未识别条目）
              └─ 用户选择后，系统标记"对方"为风格学习目标

          → AI 本地特征提取（约1-3秒，显示进度）
          → 学习完成，展示风格分析报告：
              "识别到对方消息 X 条"
              "语言风格：口语化/简洁/爱用叠词/表情多..."
              "常用口头禅：XXX、XXX"
              "平均回复长度：X 字"
          → 确认后进入聊天界面
          → 用户发送消息（对话轮数计数器可见）
          → AI 以对方风格生成回复
          → 对话自动本地保存
          → 达到 30 轮上限时提示"本次对话已达上限"
              ├─ 可【开启新对话】继续与同一角色聊天（风格特征保留，轮数重置为 0）
              ├─ 历史对话记录保留在列表中，可随时回顾
              └─ 在角色详情页可【重新导入聊天记录】重新学习，或【删除角色】换人

六、非功能需求

| 类型 | 要求 |
|------|------|
| 性能 | AI 云端回复速度 < 3 秒，本地风格特征提取 < 2 秒 |
| 隐私 | 原始聊天记录永不上云；风格特征和对话历史云端持久化（用于跨端同步），风格特征不可逆、无法还原原始对话 |
| 兼容 | MVP：iOS App + Android App（完整功能）+ 微信小程序（轻量版：示例体验 + 对话生成 + 已有对话回顾）；H5 规划中 |
| 离线 | 云端生成需联网；App 端保留本地降级模式（小模型）满足离线基础使用；小程序端无离线能力，断网时提示"需要网络连接" |
| 模型 | MVP 开发前需完成技术验证：①本地风格特征提取效果（能否准确捕捉语癖/语气等特征）；②云端生成风格模仿效果（盲测 60%+ 无法辨认真伪）；③降级模式小模型推理质量和速度 |

### 微信小程序轻量版操作流程

```
打开小程序
  → 首页：[体验示例角色] / [我的角色列表]
      │
      ├─ [体验示例角色]：直接进入聊天界面，与预设AI角色对话
      │   └─ 体验后引导下载 App 创建自己的角色
      │
      └─ [我的角色列表]：展示已在 App 端创建的角色（空状态：提示去 App 创建）
          └─ 选择角色 → 进入聊天界面，回顾历史对话 / 开启新对话（单次上限 30 轮，与 App 端一致）
```

> 小程序**不支持**：导入聊天记录、创建新角色、查看风格分析报告。这些功能需在 App 端完成，完成后自动同步至小程序。

七、风险控制与预期管理

| 风险 | 应对措施 |
|------|----------|
| 用户对"还原度"期望过高 | 产品内多处说明"风格模仿"定位，首次使用时弹窗提示能力边界 |
| 用户不清楚如何从微信导出聊天记录 | 导入引导页提供分平台操作说明（iOS/Android/PC），含截图和分步教程 |
| 粘贴的聊天记录格式不兼容导致解析失败 | 导入前自动识别 iOS/Android/PC 三种格式；不匹配时提示用户检查是否完整复制，并附格式示例对照 |
| 对话质量随轮数下降 | 单次对话上限 30 轮，鼓励新开对话 |
| 冷启动流失 | 提供"示例角色"即时体验，不需导入即可感受产品价值 |
| 依赖网络，弱网/无网时无法使用云端能力 | 保留本地降级模式（小模型离线推理），弱网时提示当前网络状态并自动切换 |
| 云端大模型 API 调用成本（免费 MVP 阶段） | MVP 阶段设置单用户每日调用上限；与云厂商签订按量付费协议，明确预算红线 |
| 风格特征和对话历史上云的合规风险（《个人信息保护法》等） | 风格特征不可逆且不含身份信息；对话历史不涉及原始聊天记录。仍须在用户协议中明确三层数据策略（原始记录/风格特征/对话历史），并取得用户同意 |
| 微信小程序审核风险（AI 对话类功能审核较严） | 小程序轻量版仅展示示例角色和历史回顾，不涉及 AI 创建/导入等敏感功能，降低拒审概率；仍须准备人审材料 |
| 用户在其他平台（小程序/H5）找不到产品 | 产品页和商店页明确标注仅支持 iOS/Android App，官网列出各端支持状态 |
| 后续收费引发反感 | MVP 即设置用量上限，用户从第一天就感知到"这是有边界的免费版" |

八、后续迭代方向（暂不做）

- 多角色切换
- 截图识别（OCR）导入
- 系统分享导入
- 聊天记录文件自动解析
- 图片/表情回复
- 语音模拟
- 付费订阅（解锁更高消息上限、更多角色、更长对话轮数）