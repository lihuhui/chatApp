# CLAUDE.md

## 交流语言

- 所有沟通、注释、文档均使用中文

## 插件协作

- **frontend-design**：涉及 UI 设计、样式优化时，可调用此插件提升设计质量
- **superpowers**：需要执行复杂任务或使用高级技能时，按插件规范调用
- **code-review**：代码修改完成后，触发代码审查检查质量

## 项目类型

- uni-app 跨端项目
- **MVP 目标平台：App 端（iOS + Android 完整功能）+ 微信小程序（轻量版）**
- 轻量版小程序：支持示例角色体验 + 已有角色的对话生成（含新对话，上限 30 轮），**不支持**创建/导入功能
- H5 规划中
- 代码保留 uni-app 跨端结构便于扩展

## 隐私策略（数据分层）

| 数据类型 | 存储策略 | 说明 |
|---------|---------|------|
| 原始聊天记录 | 永不上云，仅存本地 | 用于风格特征提取 |
| 风格特征 | 云端持久化（不可逆） | 用于云端生成回复 + 跨端同步 |
| 对话历史 | 云端持久化 | 用于跨端查看和继续对话 |

## 技术架构

- AI 方案：**本地分析 + 云端生成（混合模式）**
  - 本地：聊天记录解析、风格特征提取（NLP 文本分析）
  - 云端：大模型按风格特征生成回复（App 和小程序共用）
  - 离线降级：本地小模型兜底
- 跨端同步：风格特征和对话历史 → 云端 → App/小程序共享

## 技术栈

- 框架: uni-app (Vue 3，版本锁定在当前稳定版，不追求最新)
- 状态管理: Pinia
- 样式: SCSS
- 包管理: npm

## 依赖兼容性

- 所有依赖版本以当前项目实际锁定版本为准，不主动升级到最新
- 新增依赖前需确认与 uni-app 及各目标平台的兼容性
- 优先使用 uni-app 官方生态插件，避免引入有平台限制的第三方库

## 目录结构

- `src/pages/` - 页面
- `src/components/` - 组件
- `src/api/` - 接口封装
- `src/utils/` - 工具函数
- `src/store/` - Pinia store
- `src/static/` - 静态资源

## 编码规范

- 使用 `uni.` 前缀 API，避免平台私有 API
- 样式单位用 `rpx`
- 平台差异用条件编译 `#ifdef` / `#ifndef`
- 页面生命周期优先 `onLoad`、`onShow`

## 注意事项

- 禁止直接操作 DOM
- 注意微信小程序包体积和分包
- H5 注意跨域配置
- 图片优先使用 CDN
- 依赖升级前需在目标平台充分测试